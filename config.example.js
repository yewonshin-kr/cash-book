/**
 * 이 파일을 복사해서 같은 폴더에 `config.js`로 저장하세요.
 * config.js가 없으면 앱은 내장 데이터(오프라인 모드)로 동작합니다.
 *
 * 주의: config.js는 절대 공개 저장소에 올리지 마세요! (.gitignore에 포함됨)
 */
window.APP_CONFIG = {
  // Apps Script 배포 후 발급되는 웹 앱 URL
  url: 'https://script.google.com/macros/s/XXXXXXXXXXXX/exec',
  // Code.gs의 TOKEN과 동일한 문자열
  token: 'CHANGE_ME_TO_A_LONG_RANDOM_STRING',
};
